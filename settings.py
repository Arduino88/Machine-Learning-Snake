def init():
    global columns, rows, delay, scale, startposx, startposy
    columns = 20
    rows = 20 # Rows in the window
    delay = 100 # milliseconds between each frame
    scale = 35 # Pixels per column
    startposx = 2
    startposy = 2